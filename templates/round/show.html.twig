{% extends 'base.html.twig' %}

{% block title %}Round{% endblock %}

{% block body %}
    <h1>Round</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ round.id }}</td>
            </tr>
            <tr>
                <th>Player1Score</th>
                <td>{{ round.Player1Score }}</td>
            </tr>
            <tr>
                <th>Player2Score</th>
                <td>{{ round.Player2Score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('round_index') }}">back to list</a>

    <a href="{{ path('round_edit', {'id': round.id}) }}">edit</a>

    {{ include('round/_delete_form.html.twig') }}
{% endblock %}
