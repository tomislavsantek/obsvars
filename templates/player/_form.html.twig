{{ form_start(form) }}
<table class="table table-hover">
    <thead class="thead-light">
    <tr>
        <th>Player</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td>{{ form_widget(form.name) }}</td>
        <td><button class="btn btn-success">{{ button_label|default('Add new player') }}</button></td>
    </tr>
    </tbody>
</table>
{{ form_widget(form) }}
{{ form_end(form) }}
